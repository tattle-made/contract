export var ClientStaging = {
  mounted() {
    // section = this.el;
    // console.log(section);
    // submit_button = section.querySelector(".staging_submit");
    // submit_button.addEventListener("click", (e) => {
    //   e.preventDefault();
    //   card_divs = document.querySelectorAll(
    //     ".container>#staging>.dropzone>.item"
    //   );
    //   card_ids = Array.from(card_divs).map((x) => x.id);
    //   console.log(card_ids);
    //   // this.handleEvent("sent_from_server", (data)=>{
    //   // })
    //   // this.pushEvent("send_event", {})
    // });
  },
};
